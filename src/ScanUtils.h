#ifndef _SCANUTILS_H_
#define _SCANUTILS_H_

char CleanChar (char *inputString); // Cleans residual or escaped special characters from scanner's character string.
char* CleanString (char *inputString); // Cleans residual or escaped special characters from scanner string.

#endif